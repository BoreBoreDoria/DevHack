import { TemplateRef, ViewContainerRef, SimpleChanges, OnChanges, OnDestroy } from '@angular/core';
interface SocketIOContext {
    $implicit: SocketIOClient.Socket;
}
export declare class SocketIODirective implements OnChanges, OnDestroy {
    private templateRef;
    private vcr;
    socketIOOf: string;
    private context;
    private socket;
    private viewRef;
    constructor(templateRef: TemplateRef<SocketIOContext>, vcr: ViewContainerRef);
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    private init;
    private destroy;
}
export {};
