import { OnChanges, OnDestroy, SimpleChanges, TemplateRef, ViewContainerRef } from '@angular/core';
import 'socket.io-client';
interface SocketIOOnContext {
    $implicit: any;
}
export declare class SocketIOOnDirective implements OnChanges, OnDestroy {
    private templateRef;
    private vcr;
    socketIOOnOn: string;
    socketIOOnFrom: SocketIOClient.Socket;
    private context;
    private subscription;
    private viewRef;
    constructor(templateRef: TemplateRef<SocketIOOnContext>, vcr: ViewContainerRef);
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    private init;
    private destroy;
}
export {};
