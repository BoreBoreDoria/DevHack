import { OnChanges, OnDestroy, SimpleChanges, TemplateRef, ViewContainerRef } from '@angular/core';
export declare class RepeatContext {
    $implicit: number;
    repeat: number;
    index: number;
}
export declare class RepeatDirective implements OnChanges, OnDestroy {
    private viewContainerRef;
    private templateRef;
    repeat: number;
    repeatOf: number;
    get count(): number;
    constructor(viewContainerRef: ViewContainerRef, templateRef: TemplateRef<RepeatContext>);
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    private onRepeatChanged;
}
