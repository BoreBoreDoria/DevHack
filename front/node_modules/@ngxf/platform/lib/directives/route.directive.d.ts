import { OnDestroy, TemplateRef, Type, ViewContainerRef } from '@angular/core';
import { ActivatedRoute, ActivatedRouteSnapshot, Data, ParamMap, Params, Route, UrlSegment } from '@angular/router';
interface RouteContext {
    $implicit: ActivatedRoute;
    snapshot: ActivatedRouteSnapshot;
    url: UrlSegment[];
    params: Params;
    queryParams: Params;
    fragment: string;
    data: Data;
    outlet: string;
    component: Type<any> | string;
    routeConfig: Route;
    root: ActivatedRoute;
    parent: ActivatedRoute | null;
    firstChild: ActivatedRoute | null;
    children: ActivatedRoute[];
    pathFromRoot: ActivatedRoute[];
    paramMap: ParamMap;
    queryParamMap: ParamMap;
}
export declare class RouteDirective implements OnDestroy {
    private templateRef;
    private viewContainerRef;
    private route;
    private context;
    private viewRef;
    private subscriptions;
    constructor(templateRef: TemplateRef<RouteContext>, viewContainerRef: ViewContainerRef, route: ActivatedRoute);
    ngOnDestroy(): void;
    private attachFields;
    private asyncAttach;
}
export {};
