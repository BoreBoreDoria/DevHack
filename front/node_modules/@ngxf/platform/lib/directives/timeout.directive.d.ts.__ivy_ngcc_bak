import { OnDestroy, TemplateRef, ViewContainerRef } from '@angular/core';
export declare class TimeoutDirective implements OnDestroy {
    private templateRef;
    private viewContainerRef;
    private timeoutId;
    constructor(templateRef: TemplateRef<null>, viewContainerRef: ViewContainerRef);
    set timeout(milliseconds: number);
    ngOnDestroy(): void;
    private create;
    private dispose;
}
